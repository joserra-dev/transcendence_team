services:
  frontend:
    build:
      context: ./frontend
      target: production
    volumes: []
    ports:
      - "${FRONT_PORT}:443"
    environment:
      - URL_BACK=/
    entrypoint: ["/docker-entrypoint.sh"]
    command: []

# NOTE: For production deployment ensure URL_FRONT in .env uses https://